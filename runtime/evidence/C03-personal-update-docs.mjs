import assert from 'node:assert/strict';
import {readFileSync,writeFileSync} from 'node:fs';
const e=JSON.parse(readFileSync('evidence/C03-personal-verification.json','utf8'));
assert.equal(e.nativeLinux.status,'passed');assert.equal(e.nativeLinux.cleanup.sshClosed,true);assert.equal(e.nativeLinux.cleanup.ownedProcesses,0);
const n=e.nativeLinux.tests.pass,t=e.nativeLinux.targeted.pass;
const intro=`2026-09-07 개인 기억의 명시 등록→새 대화 회상→정정·잊기를 CLI/Web·기억 도구·실제 입력 문맥에 연결했다. NAS 실제 Linux/Node24에서 **전체 ${n.toLocaleString('en-US')}/${n.toLocaleString('en-US')}**, 신규 관련 **${t}/${t}**을 통과했다. [개인 기억 결과](/Users/seunghanee/Documents/secumon/design/chapters/C03-personal-memory-result.md) · [확정 증거](/Users/seunghanee/Documents/secumon/runtime/evidence/C03-personal-verification.json). 기본 SQLite를 유지하며 다음은 문서 기억과 명시 등록형 PostgreSQL이다. 단회 계측의 중복 읽기와 Web 오류 표시 문제는 C05/C06에 남긴다. 실제 모델/API 시험은 중단 상태이며 C03 전체와 전체 goal은 진행 중이다.`;
for(const file of ['README.md','../design/README.md','../design/03-migration-plan.md']) {
 let s=readFileSync(file,'utf8');const position=s.indexOf('2026-09-07 반복 compact');assert.ok(position>=0);
 s=s.slice(0,position)+intro+'\n\n이전 검증 단위: '+s.slice(position);
 s=s.replace('다음은 [C03 개인 기억의 등록·회상·정정·잊기](/Users/seunghanee/Documents/secumon/design/chapters/C03-personal-memory-plan.md)다.','이 검증 이후 C03 개인 기억 흐름을 진행했다.');
 s=s.replace('현재 결과와 다음 C03은','현재 결과와 C03 후속은');
 s=s.replace('**반복 의미 compact와 Windows/호스트 실행 격리는 남아 있다.**','**반복 compact의 원문·출처·정산 연결은 합성 provider로 검증했고 실제 모델 품질과 Windows/호스트 실행 격리는 남아 있다.**');
 s=s.replace('2026-09-06 · v0.51 · 검토 중 논의한 전체 범위 반영','2026-09-07 · v0.52 · C03 명시적 개인 기억 연결과 잔여 범위 갱신');
 if(file.includes('migration'))s=s.replace('### C03 — 개인 기억·이력 분리와 저장 어댑터','### C03 — 개인 기억·이력 분리와 저장 어댑터\n\n명시 등록·회상·정정·잊기 첫 흐름을 [결과](chapters/C03-personal-memory-result.md)의 범위에서 검증했다. 다음 [문서 기억 계획](chapters/C03-document-memory-plan.md)과 [PostgreSQL 검토](chapters/C03-postgres-adapter-notes.md)를 이어가며 개인 출처 유형·저장 어댑터·실제 모델 품질을 구분한다.');
 s=s.replace('현재 goal은 C02 반복 compact 구조 검증을 마치고 C03의 개인 기억 등록·새 대화 회상·정정·잊기로 이어간다.','현재 goal은 C03 개인 기억 첫 흐름을 검증하고 문서 기억·등록형 PostgreSQL로 이어간다.').replace('C02 지속 세션·반복 compact와 최신 Linux 검증 결과를 반영했다.','C03 개인 기억의 흐름·격리와 최신 Linux 검증 결과를 반영했다.').replace('2026-09-06 · v0.51 · 검토 후 전체 구현 범위와 순서 반영','2026-09-07 · v0.52 · C03 개인 기억과 후속 저장 어댑터 반영');
 writeFileSync(file,s);
}
const verification='../design/VERIFICATION.md';let v=readFileSync(verification,'utf8');const pos=v.indexOf('\n');v=v.slice(0,pos+1)+'\n## C03 개인 기억 첫 사용자 흐름\n\n'+intro+'\n\n수정본 macOS 관련52개·코어/구조143개, 같은 소스의 실제 Web 관찰과 단회 공개 포트 계측을 포함한다. 첫 시험 기대 오류3개와 broad review waiver 결함의 실패 재현·수정 기록을 보존했다. Web stale 선택 오류표시와 중복 읽기 비용은 잔여이며 운영 품질 통과로 표현하지 않는다.\n'+v.slice(pos+1);writeFileSync(verification,v);
const result='../design/chapters/C03-personal-memory-result.md';let r=readFileSync(result,'utf8');r=r.replace('이번 사용자 흐름 구현 및 로컬 검증 완료 · Linux 전체 회귀 진행 중','이번 사용자 흐름 구현 및 로컬·Linux 회귀 검증 완료');r=r.replace('| Linux 관련 시험·빌드·코어·구조 | 통과, 전체 회귀는 진행 중 | C03-personal-linux-nas-20260907의 실행 원로그 회수 예정 |',`| Linux 전체·관련 시험·빌드·코어·구조·CLI·fixture | 전체 ${n}/${n}, 관련 ${t}/${t} 통과 | [확정 증거](../../runtime/evidence/C03-personal-verification.json) |`);r=r.replace('## 다음 단위와 전체 잔여',`Linux 실행 종료 ${e.nativeLinux.finishedAt}. 원로그를 회수·해시 기록하고 전용 시험 프로세스0·SSH 종료·기존 시스템 Node18 유지·root0700을 확인했다.\n\n## 다음 단위와 전체 잔여`);writeFileSync(result,r);
const path='../design/secumon-review.html';let h=readFileSync(path,'utf8');
h=h.replace('2026.09.07 C02 설명 갱신','2026.09.07 C03 설명 갱신').replace('C02 지속 대화 구현','C03 개인 기억 연결').replace('작업이 끝나도<br>대화의 문맥을 이어간다.','필요한 기억을<br>새 대화에서도 불러온다.').replace('반복 compact와 재시작 경로를 연결했다. 아래 최신 진행 표시와 과거 검증 기록을 구분해서 읽으면 된다.','대화 문맥은 compact로 이어가고 개인 기억은 별도로 선택한다. 최신 검증과 남은 범위를 구분해 읽으면 된다.');
h=h.replace(/<div class="note gap-top" id="latest-status"[\s\S]*?<\/div>/,`<div class="note gap-top" id="latest-status" data-nas-run="29539" aria-label="최신 구현과 검증 상태"><strong>2026.09.07 · C03 개인 기억 · Linux 전체 ${n.toLocaleString('en-US')} / ${n.toLocaleString('en-US')} 통과</strong><br>지정한 발언을 기억하고, 같은 담당·사용자의 새 대화에서 선택해 불러오며 정정·잊기를 처리한다. 관련 ${t}개와 전체 회귀를 같은 소스로 검증하고 NAS 시험 프로세스·SSH를 정리했다. 검색만으로 문맥에 넣지 않는다. 실제 모델·Windows 실행·문서/PostgreSQL 어댑터는 남아 있으며 C03 전체 완료는 아니다. 호출 비용과 화면 오류표시 개선도 후속이다.<br><a href="chapters/C03-personal-memory-result.md" target="_blank" rel="noopener">C03 결과·검증 기록 →</a> · <a href="../runtime/evidence/C03-personal-verification.json" target="_blank" rel="noopener">최종 검증 근거 →</a> · <a href="chapters/C03-document-memory-plan.md" target="_blank" rel="noopener">다음 문서 기억 계획 →</a> · <a href="chapters/C02-session-compact-result.md" target="_blank" rel="noopener">이전 C02 결과 →</a></div>`);
const match=h.match(/(<script[^>]*type="application\/json"[^>]*>)([\s\S]*?)(<\/script>)/);assert.ok(match);const data=JSON.parse(match[2]);
const module=id=>data.modules.find(m=>m.id===id);const memory=module('memory');const oldSummary=memory.summary;
Object.assign(memory,{summary:'개인 기억을 별도로 저장하고, 같은 담당·사용자의 새 대화에서 필요한 기억만 선택한다.',how:['단기 문맥은 이번 입력 창, 원문 이력은 과거 대화, 개인 장기기억은 새 대화에서도 다시 쓸 기록이다. 같은 담당이라도 다른 사용자의 개인 기억은 분리한다.','지정한 사용자 발언을 기억 → 새 대화에서 검색 → 이번 업무에 사용할 기억 선택 → 정정·잊기를 CLI/Web으로 처리한다. 검색만으로 자동 회상하지 않는다.','조직·담당·사용자 범위를 정본·명령 기록·색인·참조에 적용한다. 사용 전 원문 출처와 현재 버전·권한을 확인한다.','현재의 정확한 발언 인용 규칙은 사용자 명시 기억 유형의 규칙이다. 향후 자율 경험·학습 기억도 알맞은 출처 유형을 통해 확장하며 모든 기억에 사람 입력을 강제하는 저장소는 아니다.'],done:'기본 SQLite, 명시 공유 DB의 담당/사용자 격리, 원문 출처, 실제 입력 프레임, CLI/Web 등록·회상·정정·잊기를 연결했다. 정정 전 늦은 응답을 거절하면서 사용량은 보존한다. 의무 처리 결함을 재현·수정했고 같은 소스의 신규52개와 Linux 전체를 확인했다.',left:'문서/파일 기억·등록형 PostgreSQL, 자율 선별의 실제 모델 품질, 반복 검증의 비용 개선이 남았다. 잊기는 활성 기억 철회이며 과거 원문 대화·백업의 물리 삭제를 뜻하지 않는다.',example:'X에서 “한국어로 요약” 발언을 기억한다. 새 대화 Y에서 그 기억을 선택하면 실제 입력에 들어간다. “자세한 설명도 추가”로 정정하면 옛 버전의 선택을 다시 확인해야 한다.'});
memory.files.push('runtime/src/application/personal-memory-service.ts','runtime/src/application/session-knowledge-sources.ts');memory.docs.push('design/chapters/C03-personal-memory-result.md');
module('context').how.push('명시적으로 선택한 개인 기억은 실제 입력에 별도 항목으로 조합한다. 정정·잊기 이후의 프레임 재사용과 지연 응답은 원문·버전·권한 검사를 다시 통과해야 한다.');
module('context').docs.push('design/chapters/C03-personal-memory-result.md');
Object.assign(module('storage'),{en:'Ports · SQLite · File journal · Memory scope',how:['상태·대화·원본·개인 기억의 용도를 나누고 포트로 접근한다.','기본 개인 기억은 담당별 SQLite다. 명시 등록한 공유 DB에서도 조직·담당·사용자 범위를 정본·명령 기록·색인에 적용한다.','실행 상태는 SQLite와 파일 저널을 검증했다. 파일 저널 상태 지원과 Markdown 개인 기억 지원은 서로 다르다.','기존 기억 JSON과 명령 해시를 보존해 이행하고 실제 중단 지점에서 복구한다.'],done:'실행 상태 두 저장 방식과 개인 기억 SQLite 범위·이행·중단 복구를 검증했다. 개인 기억의 소유 경계는 대화 ID가 아니므로 같은 사용자의 새 대화에서 회상할 수 있다.',left:'문서 기억·PostgreSQL 어댑터는 후속이다. 애플리케이션 범위 구분만으로 OS 파일 접근 격리·분산 운영·재해 복구를 보장하지 않는다.'});module('storage').files.push('runtime/src/infrastructure/sqlite-knowledge.ts','runtime/src/infrastructure/sqlite-knowledge-owner.ts');module('storage').docs.push('design/chapters/C03-personal-memory-result.md');
module('tools').how.push('core.memory.get/search의 개인 범위 옵션도 같은 기억 서비스와 출처 검사를 사용한다. 후보 캐시가 있어도 본문·권한을 무검증 재사용하지 않는다.');module('tools').left+=' 한 기억을 준비하는 중첩 조회 비용을 단회 계측했으며, C05에서 현재성 검증을 유지하며 줄일 필요가 있다.';module('tools').files.push('runtime/src/application/knowledge-tools.ts');module('tools').docs.push('design/chapters/C03-personal-memory-result.md');
module('channels').how.push('개인 기억 검색·선택·정정·잊기는 별도 관리 영역에 표시한다. 관리 알림을 매번 채팅 말풍선으로 저장하지 않는다.');module('channels').done+=' C03의 CLI/Web 흐름과 실제 브라우저의 저장·정정·재선택·잊기·재접속을 확인했다.';module('channels').left+=' 낡은 기억 선택을 일반 권한 오류로 표시하는 문제와 복구 뒤 오류 문구 잔존, 재접속 시 업무 선택 복원은 C06에 남긴다.';module('channels').files.push('runtime/src/presentation/local-personal-memory.ts','runtime/src/presentation/web/personal-memory.ts');module('channels').docs.push('design/chapters/C03-personal-memory-result.md');
const g=data.glossary.find(g=>g.id==='memory'),oldExample=g.example;g.example='대화 요약은 자동으로 장기기억이 되지 않는다. C03은 발언을 지정해 저장하고 새 대화에서 선택·회상하는 흐름을 연결했다.';
h=h.replace(match[0],match[1]+JSON.stringify(data).replaceAll('<','\\u003c')+match[3]);h=h.replaceAll(oldSummary,memory.summary).replaceAll(oldExample,g.example);
h=h.replace('다음 사용자 흐름은 C03의 “기억해 줘 → 새 대화에서 회상 → 정정·잊기”다.','C03의 명시 기억 → 새 대화 회상 → 정정·잊기를 연결했다. 다음은 문서/파일 기억과 명시 등록형 PostgreSQL이다.').replaceAll('다음 C03 범위 검토','C03 착수 범위 검토').replace('현재 C02 수정본의 지문과 검증 범위','현재 수정본의 지문과 검증 범위');
writeFileSync(path,h);console.log(JSON.stringify({updated:['README','plan','verification','result','review.html'],nativeTests:n,targeted:t}));
