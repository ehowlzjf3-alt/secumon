apiVersion: v1
kind: ConfigMap
metadata:
  name: secu-agent-jenkins-deployment-status
  namespace: secu-agent
  labels:
    app.kubernetes.io/name: secu-agent-jenkins
    app.kubernetes.io/component: deployment-contract
    secu-agent.io/domain: jenkins
data:
  status: plugin-only
  runnable_workloads: "0"
  current_capability: jenkins_task_scan tool and low-level Jenkins REST tools are registered by plugin/bootstrap.py.
  missing_workload: Dedicated Jenkins pipeline runner and web dashboard are not implemented yet.
  next_step: Add a Jenkins queue/application layer before creating a long-running Deployment.
