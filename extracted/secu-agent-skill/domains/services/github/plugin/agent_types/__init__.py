"""github 서비스 agent_type — GitHub Enterprise REST v3 API enumerate (httpx).

github.py = repo/blob/commit 열거(P). github_scan.py = clone+worktree/history 스캔
(git subprocess + detectors scan_text/secrets, P/G). 모두 read-only.
"""
