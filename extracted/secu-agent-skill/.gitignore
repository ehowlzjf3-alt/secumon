__pycache__/
.env
.env.local
.env.bak*
*.env.bak
*.bak.codex
*.pyc
_logs/
_evidence/
.pytest_cache/

# 검토원이 샌드박스에서 만든 빈 sqlite — `sqlite3.connect("...")` 은 실패하지 않고
# **파일을 새로 만든다**. 도구 설명에 "로컬 DB 파일은 없다" 를 넣어 줄였지만(d40caf9)
# 모델이 또 만들면 저장소가 더러워진다. 추적하지 않는다.
*.db
