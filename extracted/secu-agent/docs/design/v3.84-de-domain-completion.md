# v3.84 — de-domain 완결: 코어 도메인 누출 7건 추출

2026-07-10. 범용 agent 감사(→ [[v370-general-agent-audit]])가 확정한 **코어 도메인
누출 7건**을 통일 원리(코어=프로토콜+게이트+등록 API / 도메인=등록형 어댑터)에 맞춰
전부 추출한다. 목표: **코어(`src/secu_agent`)는 도메인-프리, skill repo
(`secu-agent-skill`)가 등록형으로 재공급.**

seam: skill `plugin/bootstrap.py::register_all()` 가 코어 `register_*` 훅을 호출
(엔진 `SA_PLUGINS` 로드). SAFETY-KEEP(SMB lockout/SSO 서킷브레이커/PII 마스킹/
url_safety/read-only) **약화 금지**.

## 신설 코어 등록 API
- `register_memory_scope(key)` — finding_taxonomy 동형. 코어 base={global,operator}. (#5)
- `register_finding_enricher(fn)` — submit_finding post-emit 훅(순회 호출). (#3)
- `register_instruction_preamble(fn)` — LLM 트랜스포트 instructions 앞단 기여 훅. (#1)
- `register_agent_type_registry(task_type, builder)` + `register_agent_type_prompt(task_type, loader)`
  — 도메인 agent_type 도구/프롬프트 공급. (#2)
- session_search kind: 기존 `register_finding_category` 재사용(=유효 kind 검증). (#4)
- scan_text 문서신호: 기존 `document_sensitivity` seam 유지 + 경로 하드코딩/description
  중립화(등록형 신호 조회). (#6)

## Wave 순서 (독립성·리스크 오름차순, 각 커밋+테스트)
- **A (코어 텍스트 중립화, core-only 위주)**
  - #7 `mcp/server.py`: `_SERVER_NAME`/`_SERVER_INSTRUCTIONS` 중립화, stale SMB/subnet/
    task_type 문구·미등록 툴 참조 제거.
  - #1 `codex_responses_client.py`: `_CODEX_CYBER_PREAMBLE` 제거 → `register_instruction_preamble`
    훅으로 이동. 코어 기본=중립. `system_generic.txt` 도메인 어휘 제거. skill 재등록.
  - #6 `scan_text.py`/`text_scan.py`: description "공정/경영자료" 중립화, 문서신호 확장을
    등록형 조회로 일반화.
- **B (신설 API + skill 어댑터)**
  - #5 MemoryScope: `register_memory_scope`. 코어 base 2종, skill=host/share/path_pattern.
  - #3 pivot: `register_finding_enricher`. `pivot.py` → skill repo, submit_finding 훅 호출.
- **C (최대 규모)**
  - #4 session_search: kind 개방(registry 검증), `finding_index` 스키마 중립화
    (share_id/host/share_name → generic location/metadata). DB 마이그레이션 동반.
  - #2 agent_type hardwiring: `register_agent_type_registry/prompt`. package_sandbox/finding_narrator
    (+ SubmitVerdictTool/EnrichFindingTool/GTI scaffold/report_writer) → skill.
    `'smb'` 리터럴 alias(session_runtime/scheduler_tick/chat_session) 제거.

## 원칙
- 각 추출 후: 코어 단독 테스트(SA_PLUGINS="" SA_SKILLS_DIRS="") green + bootstrap 재부착 round-trip smoke.
- 새 하드리밋/동작변경 없음 — de-domain 은 동작 보존(plugin 재부착 시 기존과 동일).
- 코어에 남는 것: 프로토콜 배선 + 안전 게이트(url_safety/redact_secrets/Knox delivery sink) + 등록 API.

## 진행 상태 (2026-07-10)
**완료·커밋 (코어+skill 양쪽, 코어 1618~1631 green + bootstrap round-trip 검증):**
- #7 mcp-instructions — 코어만 중립화.
- #1 codex-preamble — register_instruction_preamble 훅, skill 재등록(env kill-switch 보존).
- #6 scan_text — register_text_signal_scanner 훅, description 중립화.
- #5 MemoryScope — register_memory_scope, base=global/operator, skill 재등록.
- #3 pivot — register_finding_enricher 훅, pivot 로직 skill 이관(seam), 코어 finding_attach_pivot/
  pivot_exposed 는 중립 enrichment 슬롯으로 유지.
- #4 (부분) session_search — 코어 TOOL de-domain: kind enum→개방 str, vocab 중립화, 렌더 일반화.
- #2b report_writer — 코어 런타임 호출자 없어 skill 이관(seam) + 테스트 이관.

**남은 것 (large·cross-repo — skill 테스트 스위트 복구가 안전 검증 선결):**
- #2c agent_type 하드배선 (2026-07-11 조사로 재평가 — **성급 구현 되돌림**):
  - register_agent_type_toolset/register_agent_type_prompt 프레임워크는 프로토타입했으나 **깨끗한
    소비자가 없어 revert**. 두 하드코딩 agent_type 를 이 프레임워크로 옮기는 게 각각 막힘:
  - **finding_narrator = 의도적 코어 sub-agent** (get_agent("finding_narrator"),
    test_agent_system.py:86 "코어 패키지에는 finding_narrator 만 — 도메인 agent md 는
    plugin 소유"). 코어-tools-only(EnrichFinding 등). 감사의 '#2 → skill' 권고가 기존
    설계 의도와 **충돌** → 이관하려면 agent-def 까지 옮기고 core-vs-domain 정책을 먼저
    결정해야 함. (현 판단: finding narration 은 코어 역량일 가능성 — 유지가 맞을 수도.)
  - **package_sandbox = cli.py 9개 실행계약 분기**로 얽힘(user message builder 132/176,
    task_type 화이트리스트 520, GTI MCP 부트스트랩 555/558, turn 예산·종료조건 577/604/
    620/675/711). 도구/프롬프트 등록형화만으론 부족 — register_agent_type_execution_contract
    (user-message/MCP-bootstrap/turn-budget/termination) 프레임워크 신설이 선결.
    SubmitVerdictTool 은 도메인 전용이라 그때 함께 이관.
  - 결론: #2c 는 (a) finding_narrator core-vs-domain 정책 결정, (b) 실행계약 프레임워크
    신설, (c) skill 스위트 green(현 85%) 이 갖춰진 뒤 **전용 세션**에서 진행. 프레임워크
    자체는 위 둘이 풀리면 30분 작업.
- #2a 'smb' 별칭(session_runtime/scheduler_tick runtime_task_type): 레거시 backward-compat
  리스크 → #2c 의 registry 기반 라우팅으로 함께 처리 권장.
- #4 finding_index 스키마: share_id/host/share_name → generic scope_id/location/metadata +
  session_search API(exclude_share_id). 코어+skill(state_domain _reindex_file_review/
  _reindex_share_review) 동시 마이그레이션 + 기존 DB ALTER → 조정된 별도 변경.

skill 테스트 스위트는 선존 rot(제거된 코어 심볼 web_discovery_tool 등 참조)로 collection 실패 —
남은 cross-repo 작업의 end-to-end 검증 전에 복구 필요.
