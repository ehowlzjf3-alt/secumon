# design/ — 설계 트랙 산출물

## 읽는 순서

| # | 파일 | 내용 |
|---|---|---|
| 1 | **`v3.81-de-domain-autonomy.md`** | **v3.81 — 도메인-프리 자율 엔진.** 확정결정 6건(착수순서·수직슬라이스·enum plugin소유·UI/DB 코어밖·delivery sink (a)·outbound opt-in), 통일원리(코어=프로토콜+게이트/도메인=등록형 어댑터), 트랙 T1a~T4 로드맵 — **전부 구현 완료** |
| 2 | **`v3.82-interface-and-domain-split.md`** | **v3.82 — 인터페이스 재정비 + 도메인 서비스 분리.** 확정결정 ⑦~⑪(도메인 서비스=skill repo 실행형·DB 소유권만 이전·rich TUI·docs 청소·evidence 게이트 등록형 이동), 트랙 U1~U5 |
| 3 | **`v3.84-de-domain-completion.md`** | **v3.84 — de-domain 완결.** 코어 도메인 누출 7건 등록형 전환 + 패키지/어휘 전면 리네임(threat_hunter→secu_agent, TH_/HUNT_→SA_, hunt_type→task_type, hunter→agent_type) |
| 4 | **`v3.85-clean-plugin-host.md`** | **v3.85 — 클린 플러그인 호스트.** "도메인 추가 시 코어 0줄 수정" 목표로 남은 9개 도메인 하드코딩을 `register_*` 훅으로 전환. 적대 리뷰로 skill-facing public 심볼 back-compat 복원. 코어 1,628 green |
| 5 | **`v3.87-core-improvement.md`** | **v3.87 — 코어 실질 개선.** 약점을 떼지 말고 개선: perf(host I/O off-loop·httpx 풀) · judge 판단품질/비용(findings 접지·evidence-delta pre-gate·done-critic) · 등록형 가드레일(`register_tool_policy`) · 모델등급 적응 loop-guard(`harness_tier`) · chromium 누수(teardown·프로세스그룹·reaper). 슬라이스별 codex a2a 합심. 후속=F2 확장·F4 비전 |
| 6 | **`v3.88-state-port-p1.md`** (+ 계약 `../STATE-PORT-CONTRACT.md`, P2 배선 `p2-platform-provision-CORE-ASK.md`) | **v3.88 — StatePort persistence 포트 + 스키마 FULL-MOVE.** 코어가 구체 DB 대신 `StatePort` 포트를 소유(`PostgresStateAdapter` 구현), 네임스페이스=Postgres SCHEMA. **P1**(코어 16테이블→`core`)·**P2 platform**(횡단 5테이블→`platform`)·**R2 하드닝**(platform provision 원자화·built-in `pg_catalog` 한정·풀 reset 콜백·read-only shape 검증) 전부 구현·커밋·푸시(43edd88·67b8bd5·2b8080c). codex a2a 다라운드 적대검증. 코어 1,823 green + 실 SA_PLUGINS 스모크. 남은 것=P2 스킬 컷오버(skill_* 이관, 스킬 세션·락스텝) |

구설계(01/02 리뷰·parallel-subagent-design-v3.80)는 v3.82 U1 에서 삭제 — 계보는 git history,
v3.80 의 채택 결론은 v3.81 문서와 아래 진행 상태에 흡수돼 있다.

## 부속 원자료

- de-domain 추출 시 `secu-agent-skill/docs/design-archive/` 로 이동: `parallel-subagent-{raw-verdicts.json,digest.md}` (설계 경쟁 16에이전트 산출물), `efficiency-audit-{raw.json,digest.md}` (보존 규칙 10건 재심사 — REVERSE 2 / PARTIAL 4 / KEEP 3)

## 진행 상태

- [x] Slice0e: prompt-cache 안정 도구순서 — **커밋 2437e32**
- [x] Slice0a: evidence dir nonce — make_sub_evidence_dir (fail-closed) + task_id ts-nonce 공유
- [x] Slice0b: stale reclaim 전 phase 확대 — 도메인 reclaim 4종 신설 + paused/archived claim 즉시 해제 (30분 대기 제거)
- [x] Slice0c: spawn timeout 래퍼 + charter_ref 상속 — `base.spawn_subagent` backstop 600s (`SA_SUBAGENT_TIMEOUT_SEC`; 내부 wall 300s 위 계층) + `inherit_charter_ref` (cli 공통 주입 → spawn 2곳 상속)
- [x] Slice0d: worker_result.json 스키마 — `schema/worker_result.py` (pydantic, extra=forbid, evidence_paths `..`/절대경로 차단, ok↔rc=0 강제) + fail-closed reader(missing/too_large/parse/schema → claim 해제용 판정, no-throw·detail≤500) + 원자적 writer + clamping 빌더 `build_worker_result`(워커 except/grace 경로용 — Slice1 워커는 직접 생성 금지). 무동작 선반입 — 소비자는 Slice1 WorkerPool
- [x] 1주차 트랙: CONTRACTS.md + goal 상태전이 검증 (01 리포트 #1·#2) — ① `agent/CONTRACTS.md` 신설: 턴 컨트랙트 5종 표(plan=advisory 무벌점 fall-through / finding·execution=escalating fail-closed `contract_violation` / turn=직접 suppression / schedule=mixed + reason code 표·active_turn dead-param 명기) + goal 전이 매트릭스 + v3.80 워커 계약 8항(fail-closed·no-claim·타깃=1turn·SIGTERM grace·K 실측기반(재심사 #6 반영)·KEEP 2건 정확 귀속). ② `state.GOAL_STATUS_TRANSITIONS` + `validate_goal_status_transition()`(명시 raise, 기존 UPDATE 원자성 무간섭) + `goal_get_active` 미지 status error 로깅(반환 유지) + ralph_controller `_goal_pause_checked`/`_goal_mark_done_checked` 래퍼로 15곳 rowcount=0 silent failure 가시화(이벤트/노트 행동 무변경). status 는 string 유지(enum 기각 유지). 테스트 40개 — SQL↔매트릭스 4×4 전수 + goal_set CTE 스윕 + zombie escape hatch + race 로깅
- [x] Slice1: WorkerPool (subprocess, 롤링 as-completed) — `agent/worker_pool.py` 신규: 부모 claim 콜러블(next_spec) 순차 1회 호출(동시 claim 구조 제거, None=비영구 재질의, 종료는 None ∧ 활성0 이중조건), **spec당 WorkerCompletion 정확 1개** invariant(spawn 실패/crash/SIGKILL/취소/풀 내부 오류/claim 예외 drain 전부), backstop `SA_SUBAGENT_TIMEOUT_SEC`(0c 계층 재사용) → SIGTERM→grace(기본 5s)→SIGKILL, 결과는 0d reader fail-closed 단일 채널, stdout/stderr 는 evidence_dir 파일로만(컨텍스트 경계), PID registry + `reap_orphan_workers()`(starttime 검증 — PID 재사용 오살 금지, 리스크 #4). 웜풀 금지(KEEP #7): 1 spec = fresh subprocess. 테스트 26개(실프로세스 시그널 시맨틱 + FakeWorker 롤링/invariant). 플래그/호출부 연결은 Slice2 — 현재 미호출
- [x] de-domain 추출 (2026-06-12): 도메인/점검 콘텐츠 전부 `~/project/secu-agent-skill` 로 물리 이동 — skills/prompts/agents(Stage1) → SessionSearch·url_safety 코어 구출 + tools/__init__ 절단(Stage1.5+2+C.2) → 도메인 tools 22개·agent_types·document_sensitivity(Stage3+C.1) → schema/CLI/prompts/pyproject 표면 + ralph 도메인 phase 6종·goal_manager 도메인 빌더 적출(Stage4+4.5, 원형은 skill repo `engine_extracts/`) → CONTRACTS KEEP 일반화(세부는 skill repo `SAFETY-NOTES.md`)·설계 원자료 이동(Stage5). 재부착 plugin API 는 후속 — 그 전까지 도메인 기능 일시 상실(의도됨). 활성 도메인 goal 2건 pause 처리. 추출 플랜: skill repo `docs/extraction-plan.md`
- Slice2 (재정의) → **v3.81 트랙 1 의 종착 항목 T1d 로 흡수** (아래 v3.81 블록 참조). 범위 동일: generic fan-out 헬퍼 + 어댑터 프로토콜(claim_fn/spec_builder/summary_fn) + Fake 어댑터 테스트. ~~어댑터 6종 등록 + SMB 활성화~~ 는 재부착 후
- [ ] Slice3: 측정 게이트 (wall-clock·토큰·캐시 cross-process 적중률) — **통과 전 확대 금지**. SMB plugin 재부착 후 실측
- **v3.81 방향 확정 (2026-06-12) → `v3.81-de-domain-autonomy.md`.** 트랙 1~4 (엔진 자율성 먼저 / delivery sink+egress 게이트 / skills 외부화+계약+보일러플레이트 / 위생정리). 착수 1순위 = **termination_gap 수정**(generic max_turns=0 무한루프, 긴급·선행) → AgentTool→WorkerPool 교체 → 모델 라우팅 → Slice2.
  - [x] T1a: termination_gap 수정 (긴급) — ① 게이트 턴 `parse_fail=None`(streak 보존 → 3x 탈출구 복구) ② `no_progress_streak` 컬럼 + 연속 무진전 judge `SA_GOAL_NO_PROGRESS_LIMIT`(기본 3, 0=비활성)회 → GoalPaused ③ `goal_record_turn` 이 streak 2종 반환. CONTRACTS.md 턴기록 계약 추가. 테스트 +7 (run() 무한루프 회귀가드 포함)
  - [x] T1b: AgentTool 백엔드 → WorkerPool subprocess — ① thread(`spawn_subagent`, kill 불가) → WorkerPool(k=1) subprocess(취소/backstop 시 SIGTERM→grace→SIGKILL 실제 종료) ② `cli.main` 이 모든 종료 경로에서 worker_result.json 작성(fail-closed 유일 채널) + SIGTERM grace 에 error_cancel 기록 ③ 재귀 depth 제한 `SA_AGENT_DEPTH`/`SA_AGENT_MAX_DEPTH`(기본 2) ④ 결과에 turns/tokens/duration 가시화. 워커 PYTHONPATH=부모 src 고정(editable install 어긋남 방지). 테스트: fake 워커 실subprocess 기반 재작성 +6
  - [x] T1c: 모델 버전별 라우팅 — ① cli `SA_CHAT_PROFILE` 무시 버그 수정(`_select_profile_name`: argv > env > YAML 첫 항목, 무효 env 는 rc=2 fail-closed — silent downgrade 금지) ② `make_role_client(role, default=)`: `SA_JUDGE_PROFILE`/`SA_SUMMARIZER_PROFILE` 로 judge·압축 모델 분리(미설정=세션 client, 실패 fail-open, process 캐시) ③ agents/<name>.md frontmatter `profile:` → AgentTool 이 워커에 `--profile-name` 전달(sub-agent 별 모델) ④ per-worker 는 WorkerSpec.env SA_CHAT_PROFILE/SA_BEDROCK_MODEL. 진짜 턴단위(LLMRequest.model/ClientRouter)는 보류
  - [x] T1d (=Slice2 재정의): `agent/fanout.py` 신규 — ① `FanoutAdapter` 프로토콜 4-hook(claim_next/build_spec/release/summarize, async) + 등록 레지스트리(중복=명시 에러, 재부착 plugin API 첫 조각) ② `run_fanout` 롤링 기계: 부모 선claim·타깃=1turn(`parse_fail=None` judge streak 비간섭)·fail-closed→release(success=False)·max_targets 예산캡·cancel SIGTERM 전파 ③ **결정론 종료 복원**: claim None ∧ 활성0 → `report.exhausted` (루프 통합부의 judge-무관 GoalDone 신호) ④ 경량 `WorkerCompleted`/`FanoutCompleted` 이벤트. Fake 어댑터 테스트 9개. **도메인 어댑터 6종 등록·활성화·루프 통합은 재부착 후**
  - [x] T2: delivery sink 프로토콜 + egress 게이트 — `agent/delivery.py` 신규: `deliver(sink_id, payload)` + `DeliverySink` 등록형 어댑터(중복=에러) + egress 게이트(dry-run 기본 / 자율발송 4조건: sink opt-in·charter·수신자 allowlist 필수·마스킹 후 스캔 잔존 0, 스캐너 사망=차단 fail-closed / sink 는 redacted payload 만 수신). `knox/mail_sink.py` 첫 sink(owner_mail 검증·발송 일반화, 에이전트호출형). `deliver` 도구(operator). draft+`delivery_audit.jsonl` 기록. 테스트 17개
  - [x] T3: skills 플랫폼 — ① `resolve_skills_dirs()`: 코어 + `SA_SKILLS_DIRS` 다중 외부 경로(재부착 전제조건), `load_skills_all` first-wins dedup(외부가 코어 skill override 불가 fail-safe), 호출부 4곳 교체(prompts/skill_tool/chat_session×2) ② per-skill `safety.md` 동반 주입: trigger 자동 주입 시 `skill_body_with_safety` 로 도메인 안전 계약이 playbook 과 반드시 함께 ③ `register_skill_unlock_tools` plugin API(중복=에러) ④ scaffold+lint: `secu-agent skill new|lint` — name 불일치(silent skip 1순위 함정)·frontmatter 오류·32KB cap·trigger 정규식·충돌 전수 가시화, 4종 resource stub ⑤ WebFetchTool(`web_fetch`): url_safety 게이트 재사용(GET-only·redirect 미추적·bs4 없는 HTML 텍스트 추출). **WebSearch 는 보류** — 사내 검색 백엔드 미정(백엔드 결정 후 web_fetch 패턴으로). 테스트 +25
  - [x] T4: 위생/등록형 전환 — ① 보안위생: `.env.example` 실IP/실대역 마스킹, `owner_mail` 기본 호스트 제거(미설정=fail-closed), finding.py·browser_tool 실호스트 주석 일반화, 테스트 fixture CIDR 교체 ② 확정 결정 ③ 구현: `FindingCategory`→str+베이스 7종(`finding_taxonomy.register_finding_category` plugin API, 미등록=라벨 원문·우선순위 0), `HitCategory`→str, agent_type 등록형(`agent_type_registry`, 코어='agent' — chat/scheduler/schedule_tool/state 4곳 교체), orphan `findings.py` 삭제 ③ 도메인 텍스트 예시 일반화(submit_finding category 안내·smb 도구 예시→skill 가이드 참조, schema/finding·narrator·text_scan 예시). **잔여(의도)**: evidence_judgment `_judge_smb_credential_hit`(task_type 게이트 휴면)·semantic_validation 도메인 키워드 시그널·`_CONTENT_EVIDENCE_CATEGORIES` 의 legacy 분류 2종(증거 게이트 보수 유지) — plugin 재부착 시 이전, 수직 슬라이스(smb_reports/domain_reports/owner_mail SMB draft)는 결정 ② 대로 재부착과 함께 이동 ④ README 재작성 (도메인-프리 코어 기준)
- **v3.82 확정 (2026-06-13) → `v3.82-interface-and-domain-split.md`.** 확정결정 ⑦~⑪ (도메인 서비스=skill repo 내 실행형 / DB 소유권만 이전·데이터 유지 / TUI=rich REPL / docs 전체 청소 / evidence 게이트 등록형 이동). skill repo 재배치 완료(`49079cd`)로 병렬 세션 제약 해제 — 엔진+skill repo 단일 트랙.
  - [x] U1: docs 대청소 — plans/ 47개 + 도메인 docs 2종 + 구설계 3종(01/02/v3.80) + 루트 mockup 삭제, CONTRACTS·test_fanout 의 v3.80 포인터를 v3.81/git history 로 교정
  - [x] U2: SA_PLUGINS 부트스트랩 — `plugins.py`(콤마/pathsep 구분, dotted 모듈 또는 .py 파일 경로, 멱등, import 실패=PluginLoadError fail-loud) + 진입점 3곳(cli.main rc=2 / agent 워커 _run rc=2+worker_result / web create_app 전파=미기동) + .env.example 문서(임의 코드 실행 경고). 테스트 9개
  - [x] U3: 8765 분리 — 코어 슬림화 + 등록형 훅 + skill repo 도메인 서비스 (U3a~U3d 완료. U3d `fd8aca3`: service/app.py 독립 FastAPI 8766 — smb/domains/domain_reports/findings_domain 라우터, 재배선 완료, skill repo 220 green)
    - [x] U3c: state 분리 — 도메인 테이블 12종 DDL+함수 ~2,300줄 → skill repo `service/state_domain.py`(자체 lazy bootstrap, 코어 connect 풀 재사용, register_idless_table). entity_timeline generic 재작성+`register_timeline_source` 훅, cli `status`(SMB)·mcp 도메인 도구 3종 제거, `_MIGRATE_ORDER`(stale) 삭제. 도메인 state 테스트 17파일+seed fixture → skill repo service/tests/state/(151 green), 엔진 적응 7파일. 엔진 1430 green
    - [x] U3b: 8765 코어 슬림화 — 도메인 라우터 3종+services 7종+UI 도메인 렌더(3680→2161줄) 분리, generic finding 목록 신규, web/auth.py(헤더 인증+무인증 라우트 수리), agent_type 'smb' 기본값 sweep→'agent', canonical_task_type 등록형 (`ad93159`, 1576 green)
    - [x] U3a: 등록형 훅 4종 + knox 전송부 이동 — ① `register_evidence_judge(task_type)`(SMB credential 심층판정+프린터 INI 휴리스틱 → skill repo `plugin/smb_evidence_judge.py`, 결정 ⑪ 동시이동) ② `register_finding_category(requires_content_evidence=)`(legacy 분류 2종 literal 제거→plugin 등록) ③ `register_sensitive_term_signal`(semantic_validation 도메인 시그널 등록형, detectors.sensitive_terms import 방식 폐지) ④ `register_browser_verified_task_type`(submit_finding 게이트 하드코딩 tuple 제거) ⑤ Knox Mail 전송부 `web/services/owner_mail.py`→`knox/owner_mail.py`(mail_sink 의존역전 해소, draft builder 는 과도기 잔류→U3b 이동). skill repo `plugin/bootstrap.py`(SA_PLUGINS 대상, agent_type 5종+분류 2종+어휘시그널+judge+browser게이트 4종+unlock 매핑) + 행동 테스트 5종 이동
  - [x] U4: TUI `secu-agent chat` — `agent/chat_repl.py`(rich REPL: 세션 list/resume/new·스트리밍 렌더+턴말미 마크다운 정돈·keystroke 승인 resolver(audit 동일)·/compact(웹 동일 시맨틱)·Ctrl+C 턴취소·시작 배너) + `python -m secu_agent` 진입점. **`-s` 단일 resolver**: `SkillsSelection`(names=dedup 후 필터·미존재 fail-loud / extra_dirs=additive·core-first 유지) 이 3개 로드 경로(시스템프롬프트 인덱스/trigger 자동주입/skill 도구) 공통 통과, ChatSession.load(skills_selection=) 세션 고정, skill_tool metadata['skills_dir'] 교체 시맨틱 폐기(additive 교정). 테스트 +17, 1447 green
  - [x] U5: GUI 채팅 셸 개선 — ① REST 인증 헤더 전환(UI Authorization Bearer + 전 라우트 `require_token` 의존성 일괄 — chat/approvals/scheduler/evidence 의 쿼리 전용 검사 제거, WS subprotocol 유지) ② 마크다운 렌더(escape-then-transform sanitize, 외부 lib 0, 스트리밍은 평문→턴말미 재렌더) ③ goal 라이프사이클 1급 배너(분해/일시정지+이어서진행 버튼/완료) ④ 세션 rename·보관 브라우저·복원 ⑤ agent_type/skill 선택자(`chat_session.settings` JSON 컬럼 + POST/PATCH skills 검증 400 + WS 턴별 SkillsSelection 적용) ⑥ 프로필 칩 `/api/profile` API화(하드코딩 제거) ⑦ WS 자동 재연결(지수 backoff 1s→30s) ⑧ 승인 모드 토글. 테스트 +13(웹 87), 전체 1460 green
