# C06~C10 구현 후 검증 계획

2026-09-08. **먼저 C06 → C07 → C08 → C09 → C10 기능을 구현하고, 아래 검증과 수정을 수행한다.** 이 문서는 검증 대기 목록이며 현재 제품 통과 보고서가 아니다. 기존 시험/기록을 재사용하고 새 통합 소스에서 확인한 결과만 판정한다.

각 항목은 검증 시 통과/실패/미검증, 대상 소스·환경, 실행 결과 링크만 기록한다. 중단된 실제 모델/API 시험은 재개하지 않으며 외부 연결이 필요한 항목은 사유와 함께 미검증으로 유지한다.

<a id="c06"></a>

## C06 — 대화·설치·업무 배치

2026-09-08 checkpoint376: 아래 요구의 로컬 선택 시험 **고유16개가 통과**했다. Knox 직접 입구12, 두 담당 배치·기억2, 격리 설치2이며 [실행별 결과](C06-ordered-verification-result.md)를 따른다. 접수/결과·HTTP 자산 확인을 실제 브라우저 조작으로, 로컬 대역을 실제 Knox로 확대하지 않는다. 체크박스는 브라우저·현재 대상 OS·최종 통합까지 포함한 전체 인수 상태로 남긴다. 다음 로컬 챕터는 C07이다.

- [ ] V06-01 접수 사실·필요 질문·의미 있는 진행·최종 결과를 구분하고 도구 호출마다 채팅 말풍선을 늘리지 않는다. 세부 기록은 펼쳐서 본다.
- [ ] V06-02 CLI/Web 재접속·중복 전달·취소·상태 조회와 사용자/담당/세션 라우팅에서 누락·중복·문맥 혼합을 확인한다.
- [ ] V06-03 Knox 어댑터의 지원 능력과 전송/수신/재개 계약을 확인한다. 대역 채널과 실제 Knox 연결 결과를 구분한다.
- [ ] V06-04 전역 명령 설치·버전 표시·첫 setup·기존 담당 이어가기와 제거/재설치 시 자료 보존을 확인한다.
- [ ] V06-05 두 업무를 설정/도구/선택 스킬로 배치하고 공통 엔진 수정 없이 단일 담당을 사용한다. 협업 기능은 꺼진 상태도 확인한다.

<a id="c07"></a>

## C07 — 게시판·아카이브

- [ ] V07-01 허용된 참여자의 읽기·답변에 불필요한 승인/가공 절차가 없으며 선택 반응과 명시 질문의 응답 책임을 구분한다.
- [ ] V07-02 수락/거절/답변/추가 정보/근거 부족/미응답 상태와 필요한 담당만 받는 알림을 확인한다.
- [ ] V07-03 조회 전용/조회·등록 아카이브 공급자, 중복 사례·출처 버전·정정/삭제를 확인한다.
- [ ] V07-04 공유 원문 접근과 개인 기억 경계를 유지하며 조회 자료를 개인 장기기억에 자동 복사하지 않는다.
- [ ] V07-05 게시판·아카이브가 비활성이어도 지속 세션·개인 기억·작업 복구가 동작한다.

<a id="c08"></a>

## C08 — 동료·반론·자원

- [ ] V08-01 동료 양방향 요청, 상주/임시 역할, 독립 자원/위임 자원을 확인한다. 모든 질문에 자식 작업을 강제하지 않는다.
- [ ] V08-02 협업 관계·비용 부담·자료 접근·모델 위치를 독립 설정하며 고정 리드–워커 구조를 요구하지 않는다.
- [ ] V08-03 별도 문맥 반론의 대상·대안·근거 유무·판별 질문·영향·버전이 후속 계획/관측으로 이어진다. 모델 간 동의를 독립 증거로 세지 않는다.
- [ ] V08-04 에이전트의 배정·추가 요청·회수/재배정과 런타임 정산·미사용 반환을 확인한다. 취소·늦은 사용량·권한 철회 후 중복 정산이 없다.
- [ ] V08-05 compact 후 협업/장부가 이어지고 임시 역할이나 작업 종료가 상주 담당의 세션을 종료하지 않는다.
- [ ] V08-06 내부 peer 채널 답변이 SQLite/PostgreSQL에서 원 세션·전달 영수증으로 확인된다. 외부 메신저 발신과 분리하고 중복 답변/다른 담당의 세션 기록을 허용하지 않는다.

<a id="c09"></a>

## C09 — A2A·사건 재개·상시 임무

- [ ] V09-01 외부 에이전트의 작업/상태/산출물/오류/취소 계약을 확인하며 실제 상호운용과 합성 시험을 구분한다.
- [ ] V09-02 예약·관측·게시판 구독·회신 대기를 저장된 사건/커서로 재개한다. 대기 중 모델 추론을 반복하지 않는다.
- [ ] V09-03 중복/늦은 알림·부분 답변·재전달·취소·상대 장애·프로세스 재시작에서 업무 상태와 응답 의무를 유지한다.
- [ ] V09-04 무진전 대화·재계획·중복 확산을 제한하며 담당의 지속 ID/세션과 프로세스 수명을 구분한다.
- [ ] V09-05 같은 문제의 단독/협업 품질·지연·비용을 비교한다. 합성 결과로 실제 모델의 판단 품질이나 운영 비용을 확정하지 않는다.

<a id="c10"></a>

## C10 — 설치·버전·복원

- [ ] V10-01 패키지·사내/오프라인 설치·담당별 버전 고정·명시 업데이트와 엔진/설정/저장/확장 호환 검사를 확인한다.
- [ ] V10-02 업데이트 후 같은 담당·세션·기억을 이어가며 미지원 버전과 이행 실패를 명확히 처리한다.
- [ ] V10-03 지원되는 되돌리기와 DB 복원의 차이, 자료 보존, 진행 중 외부 효과 대조·중복 실행 방지를 확인한다.
- [ ] V10-04 SQLite/파일/등록형 PostgreSQL의 구현된 지원 범위에서 복원·정합성·삭제/철회·용량과 오류 처리를 확인한다.
- [ ] V10-05 검증한 실제 구성만 시범/배포 대상으로 삼는다. 운영 이관은 선택 사항이며 자동 실행하지 않는다.
- [ ] V10-06 PostgreSQL 이관 prepare/import/원자료 재쓰기 차단/activation 각 중단점에서 같은 operation으로 재개한다. revision·업무/기억/대화 영수증·session/task context head·문서 개인 기억을 보존한다. 기존 로컬 writer, DB maintenance 및 설치 스키마 2의 이전 엔진 차단을 확인한다.
- [ ] V10-07 PostgreSQL snapshot+로컬 원문 백업의 같은 시점·페이지 지문·용량·manifest 마지막 게시, 중간 파일과 실패 정리·COMMIT 불명 차단을 확인한다.
- [ ] V10-08 복원 표식의 일반 open 차단, 원 경로/담당/등록과 독립 host floor, 빈 DB/같은 operation 재시도, 늦은 외부 효과·삭제/철회 자료 대조와 업무 중복 실행 방지를 확인한다. 복원 후 과거 migration 관리 명령의 거절과 실제 업무 영수증 보존을 구분한다.
- [ ] V10-09 Windows의 현재 ABI4 링크/Node 로드와 setup/profile/clone/문서 기억 게시의 handle·ACL·reparse·동시 변경을 확인한다. process-crash와 directorySynced:false를 전원 장애 내구성 통과로 바꾸지 않는다. 일반 journal/artifact/workspace/SQLite/lifecycle은 checkpoint361에서 연결했고 관리·공유 저장 포트도 checkpoint362에서 연결했으며 실제 실행은 미검증이다.
- [ ] V10-10 checkpoint362의 ABI4를 기준으로 기존 ABI2/3 fixture 기대값을 검토하고 native 링크/로드를 확인한다. journal 기본64MiB 중4MiB 초과 기록, 1GiB 파일/4GiB tree 한도, 빈 파일, prepare/게시/close 오류와 unknown 보존을 검증한다. 일반 journal/artifact/workspace/SQLite/lifecycle은 이번에 연결했고 checkpoint362의 관리·공유 저장 포트도 함께 검증한다.
- [ ] V10-11 Windows SQLite의 같은 원 main 파일 유지, WAL/SHM 생성·삭제·동시 사용, 읽기 전용 ACL 비변경과 쓰기 준비의 동일 SID 상속을 확인한다. workspace 프로세스 종료 시 잠금 해제, lease exact bytes 삭제 및 외부 호스트/살아 있는 PID의 유지, 설치/복원 marker의 일반 open 차단을 확인한다.

- [ ] V10-12 Windows 개인 기억 snapshot/fence와 같은 연결 backup, 후보 identity를 보존한 no-replace 게시, 256MiB·4회·60초 한도 및 마지막 완료 영수증을 확인한다. POSIX 기존 backup 결과를 Windows 통과로 대체하지 않는다.
- [ ] V10-13 Windows 저널 PostgreSQL 이관에서 원 format 보존 이동 전후·새 format 부분 쓰기/게시 뒤 중단과 같은 operation 재개를 확인한다. 원 source manifest·retirement·유지보수 차단을 함께 대조한다.
- [ ] V10-14 Windows 일반/PG 복원의 marker·원문 SHA·정확한 후보 prefix·metadata 재열기, 동시 복원 lock, candidate/target 충돌 무삭제와 한도/close 오류를 확인한다.
- [ ] V10-15 Windows 공유 게시판·knowledge 등록·archive의 native 파일/SQLite 경계, 원 영수증, 재열기와 개인 자료 비합침을 확인한다.
- [ ] V10-16 명시 작업공간 복구에서 checkpoint의 현재 권한·세대·원 영수증·artifact hash를 대조하고 새 디렉터리에만 재구성한다. 원 `.lock`/`.pending` 무변경, source/destination 겹침 거절, 실패 목적지 보존과 완료 manifest 없는 open 거절을 확인한다. 복구 뷰의 업무·시도·경로 제한과 동일 내용 stage, host/profile 종료 중 drain, 원 업무/자원/실행 상태 비변경을 확인한다.
- [ ] V10-17 복원으로 새 파일 객체가 된 담당은 일반 open에서 기존 ID로 자동 합류하지 않는다. local/PG 원백업·완료 marker·원 경로·현재 원문·offline·등록 head를 확인한 명시 rebind, 같은 operation 재시도, 다른 backup/객체/기존 head 충돌을 확인한다. 다음 백업에 과거 복원 완료 marker가 포함되지 않는지, marker 없는 과거 local 복원을 완료로 추정하지 않는지도 확인한다. 호스트 등록표는 담당 backup/clone과 분리한다.

- [ ] V10-18 PostgreSQL 담당의 호스트 `check/pin/update`: PG 지원 선언 없는 과거 release·실제 installation/binding/purpose 버전 불일치 거절, 선택하지 않은 로컬 저장소/문서 기억 호환, local+DB 관리 잠금, 최초 pin·같은 pin 재확인·expectedPrevious 충돌을 확인한다. 업데이트의 결합 백업과 현재 DB 페이지·로컬 원문·등록·현재 pin을 대조하고 오래된 백업을 거절한다. no-replace pin 게시·중단 후 실제 head 조회·불명 DB commit에서 fence 유지 및 정상 종료 시 해제를 확인한다. 버전 전환으로 업무·세션·DB 내용을 재생성하거나 미확정 외부 효과를 완료 처리하지 않는지 확인한다. 예제 명령 연결·합성 검증·실제 PostgreSQL 결과는 구분한다.

## 통합 확인 순서와 보존한 잔여

1. C01~C05 기반의 필수 회귀와 이번 통합에 영향을 주는 저장·지속 문맥·추론 루프·도구 경계를 확인한다. 이미 보유한 시험과 C05 crash/drain 후보를 재사용한다.
2. C06 → C07 → C08 → C09 → C10 항목을 검증하고 발견한 문제를 수정한다. 수정 영향 범위를 다시 확인한다.
3. 전체 회귀와 실제 Linux/native Windows의 파일·프로세스·설치·연동, 저장 공급자 간 통합을 묶어 확인한다. macOS/WSL 결과를 native Windows 통과로 대체하지 않는다.
4. 실제 모델/API는 중단 상태로, 사내 MCP·Knox·실제 A2A·배포는 필요한 외부 조건과 확인 전까지 미검증으로 남긴다.

C05 응답 보관·정산 A/B 로컬 통과 결과는 보존한다. C 실제 강제 종료·프로필 종료 후보는 미통합/미실행이며 일반 CLI/HTTP 후속과 새 소스의 최종 Linux 회귀도 남아 있다. C01~C05 미구현 사항은 기존 backlog에서 유지하고 검증만 미룬 것으로 바꾸지 않는다.

C06 구현 후 구체화(Checkpoint353, 미실행): Knox 접수→실행 분리, 원격 전달 성공/unknown/lookup absent와 경로별 재시도 보장, 확인된 응답의 세션 기록·재시작, 일반 CLI pause/cancel/resume·동일 입력 재전달, 최초 file-journal 선택의 중단 복구, 포함된 fixture/Web assets의 실제 설치 소비를 V06 항목에서 확인한다.

C07 구체화(Checkpoint354, 미실행): feature false에서 등록 미개설, read_only 능력/명시 쓰기 허가 분리, 모델 register/revise/delete와 명령 digest·중복·출처 버전, 조회 자료의 개인 기억 비복사, 서로 다른 담당 DB/scope의 공유 board와 owner 고정 원출처 확인을 V07에서 점검한다. 공유 board 원출처와 archive unknown 영수증 대조는 Checkpoint355에서 제품에 연결했고 상세 시험은 이 목록에서 수행한다.

C08~C10 구현 중 추가한 검증(미실행):

- C07: 같은 원출처의 여러 경로 참조, source 등록 해제/공유 grant 철회, DB별 동일 workId 충돌과 유한 그래프 제한.
- C08: peer/local 내부 채널, 일반 동료 지속 세션과 요청별 반론 문맥, 의견의 증거 비승격, 목적지 권한, sponsor/recipient 원장 분리, 추가 요청·부분 증액·자진 반환·늦은 사용량·재시작 후 동일 원장 재등록.
- C09: A2A 양방향 지원 subset·caller별 격리·remote ack와 업무 완료 구분, 새 사건의 새 work와 동일 세션 유지, 오래된 중복 사건의 원영수증 대조, 대기 중 모델 호출0, driver 종료 후 store close, 단독/협업 품질·지연·원사용량 합산 비교.
- C10: 설치 묶음 지문·버전 CAS·호환 오류, 실제 Linux/native Windows 설치, 실행중 백업 차단·잔여 lease 회수, 원경로 복원·자료/세션 유지·미확인 외부 효과의 재실행 방지. PostgreSQL snapshot+로컬 자료 복원은 checkpoint360에서 연결했으며 V10-06~09는 미실행이다. 지원 범위 밖 일반 외부 시스템의 자료 복원을 구현했다고 표시하지 않는다.
